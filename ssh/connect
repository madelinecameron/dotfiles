declare -A rawIPs

rawIPs["dips"]="140.82.63.74"
rawIPs["qnzl"]="45.76.2.252"

function connect_ip() {
  ssh root@${rawIPs[$1]} -A -i ~/.ssh/qnzl
}

function ips() {
  for site in ${!rawIPs[@]}; do
    echo ${site} = ${rawIPs[${site}]}
  done
}
